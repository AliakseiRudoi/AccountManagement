<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">

	<!-- DataSource -->

	   <bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${dbt.driver}" />
		<property name="url" value="${dbt.url}" />
		<property name="username" value="${dbt.username}" />
		<property name="password" value="${dbt.password}" />
	</bean>

	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	<tx:annotation-driven proxy-target-class="true"
		transaction-manager="transactionManager" />  

		<context:property-placeholder location="classpath:dbt.properties" />
		
		<!--   <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
   		<property name="driverClassName" value="${dbt.driver}" />
		<property name="url" value="${dbt.url}" />
		<property name="username" value="${dbt.username}" />
		<property name="password" value="${dbt.password}" />
        <property name="maxWaitMillis" value="20000"/>
        <property name="initialSize" value="2" />
    </bean>  --> 
    
    <!-- <context:property-placeholder location="classpath:dbt.properties" /> -->
    <!-- <context:property-placeholder location="classpath:db_test.properties" /> -->



</beans>